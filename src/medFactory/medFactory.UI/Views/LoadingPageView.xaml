<UserControl x:Class="medFactory.UI.Views.LoadingPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:medFactory.UI.Views"
             mc:Ignorable="d" 
             MinHeight="600" 
             MinWidth="1000"
             Style="{StaticResource LoadingPageBackground}"
             >
    <Grid>
        <Image
            Source="/Images/logo.png"
            Style="{StaticResource Logo}"/>
        <ProgressBar
            Style="{StaticResource {x:Type ProgressBar}}"
            />
    </Grid>
</UserControl>
